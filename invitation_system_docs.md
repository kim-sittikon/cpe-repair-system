# 📋 แผนการพัฒนาระบบเชิญผู้ใช้งาน (User Invitation System)

## 🎯 ภาพรวมระบบ

ระบบนี้จะทำให้ **Admin สามารถเชิญผู้ใช้งานใหม่ผ่านอีเมล์** โดยผู้ใช้จะได้รับลิงก์เฉพาะตัวเพื่อตั้งรหัสผ่านและลงทะเบียนเข้าสู่ระบบ

---

## 🔍 วิเคราะห์โครงสร้างปัจจุบัน

### ✅ สิ่งที่มีอยู่แล้ว:
- ตาราง `accounts` พร้อมฟิลด์: email, password_hash, role, permissions (job_repair, job_admin, job_complaint)
- Laravel Authentication (ใช้ Laravel Breeze)
- Inertia.js + React สำหรับ Frontend
- Mail configuration (ปัจจุบันใช้ `MAIL_MAILER=log`)

### ⚠️ สิ่งที่ต้องเพิ่ม:
- ตารางใหม่เก็บข้อมูล invitation tokens
- Controllers จัดการ invitation flow
- Email templates สำหรับส่งคำเชิญ
- Frontend pages สำหรับ Admin และ Guest
- Routes เพิ่มเติม

---

## 📊 ขั้นตอนการทำงานทั้งหมด (Flow Diagram)

```
┌──────────────────────────────────────────────────────────────────┐
│  STEP 1: Admin เชิญผู้ใช้                                         │
├──────────────────────────────────────────────────────────────────┤
│  Admin → กรอกฟอร์ม (Email, Role, Permissions)                    │
│       → กด "ส่งคำเชิญ"                                            │
│       → ระบบสร้าง Token (60 ตัวอักษรแบบสุ่ม)                      │
│       → บันทึกลงตาราง user_invitations                            │
│       → ส่งอีเมล์พร้อม Link ไปที่ผู้ใช้                           │
└──────────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────────┐
│  STEP 2: ผู้ใช้ได้รับอีเมล์                                       │
├──────────────────────────────────────────────────────────────────┤
│  Email → มีข้อมูล: Role, Permissions, วันหมดอายุ                 │
│       → มีปุ่ม "ตั้งค่ารหัสผ่าน" (ลิงก์ไปหน้า /invite/{token})   │
│       → ผู้ใช้คลิกลิงก์                                           │
└──────────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────────┐
│  STEP 3: ระบบตรวจสอบ Token                                        │
├──────────────────────────────────────────────────────────────────┤
│  System → ตรวจสอบ Token ว่ามีอยู่จริงหรือไม่                     │
│         → ตรวจสอบว่าหมดอายุหรือยัง (72 ชั่วโมง)                  │
│         → ตรวจสอบว่ายังไม่ถูกใช้งาน (accepted_at = null)         │
│         ✅ ถูกต้อง → แสดงฟอร์มตั้งรหัสผ่าน                       │
│         ❌ ไม่ถูกต้อง → แสดง Error message                        │
└──────────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────────┐
│  STEP 4: ผู้ใช้กรอกข้อมูลและตั้งรหัสผ่าน                         │
├──────────────────────────────────────────────────────────────────┤
│  Form → Email (แสดงอัตโนมัติ, ไม่สามารถแก้ไขได้)                 │
│      → ชื่อจริง                                                   │
│      → นามสกุล                                                    │
│      → รหัสผ่าน (อย่างน้อย 8 ตัวอักษร)                           │
│      → ยืนยันรหัสผ่าน                                             │
│      → กด "ลงทะเบียน"                                             │
└──────────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────────┐
│  STEP 5: ระบบสร้างบัญชีผู้ใช้                                     │
├──────────────────────────────────────────────────────────────────┤
│  System → บันทึกข้อมูลลงตาราง accounts                           │
│         → เซ็ต Role และ Permissions ตามที่ Admin กำหนด           │
│         → อัพเดท accepted_at ในตาราง user_invitations            │
│         → ส่ง Redirect ไปหน้า Login พร้อมข้อความ "สำเร็จ"         │
└──────────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────────┐
│  STEP 6: ผู้ใช้ Login เข้าสู่ระบบ                                 │
├──────────────────────────────────────────────────────────────────┤
│  User → ใช้ Email และ Password ที่ตั้งไว้เข้าสู่ระบบ             │
│      → เข้าสู่ Dashboard ได้ทันที                                 │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ ส่วนที่ 1: ฐานข้อมูล (Database)

### 📌 ตารางใหม่: `user_invitations`

**ไฟล์:** สร้างด้วยคำสั่ง
```bash
php artisan make:migration create_user_invitations_table
```

**โครงสร้างตาราง:**

| Column | Type | Description | Example |
|--------|------|-------------|---------|
| `id` | BIGINT | Primary Key | 1 |
| `email` | VARCHAR(255) | อีเมล์ที่ถูกเชิญ (มี index) | `user@example.com` |
| `token` | VARCHAR(255) | Token เฉพาะ (Hashed, Unique) | `abc123...xyz789` |
| `role` | ENUM | บทบาท: admin, student, staff, teacher | `staff` |
| `permissions` | JSON | สิทธิ์งาน | `{"job_repair": true, "job_admin": false}` |
| `invited_by` | BIGINT | Foreign Key → accounts.account_id | 5 (admin ที่เชิญ) |
| `expires_at` | TIMESTAMP | วันหมดอายุ (72 ชั่วโมง) | `2025-12-20 10:00:00` |
| `accepted_at` | TIMESTAMP | วันที่ยอมรับคำเชิญ (null = ยังไม่รับ) | `2025-12-18 14:30:00` |
| `created_at` | TIMESTAMP | วันที่สร้าง | `2025-12-17 10:00:00` |
| `updated_at` | TIMESTAMP | วันที่แก้ไขล่าสุด | `2025-12-18 14:30:00` |

**Migration Code:**

```php
Schema::create('user_invitations', function (Blueprint $table) {
    $table->id();
    $table->string('email')->index();
    $table->string('token')->unique();
    $table->enum('role', ['admin', 'student', 'staff', 'teacher']);
    $table->json('permissions'); // {job_repair: bool, job_admin: bool, job_complaint: bool}
    $table->foreignId('invited_by')
          ->constrained('accounts', 'account_id')
          ->onDelete('cascade');
    $table->timestamp('expires_at')->index();
    $table->timestamp('accepted_at')->nullable();
    $table->timestamps();
});
```

**คำอธิบาย:**
- **`token`**: เก็บค่า Hash ของ token เพื่อความปลอดภัย (ไม่เก็บ plain text)
- **`permissions`**: เก็บเป็น JSON เพื่อความยืดหยุ่น สามารถเพิ่มสิทธิ์ใหม่ในอนาคตได้
- **`invited_by`**: เชื่อมกับ accounts เพื่อรู้ว่า Admin คนไหนเชิญ
- **`expires_at`**: กำหนดอายุ 72 ชั่วโมง ถ้าเกินเวลานี้จะใช้ไม่ได้
- **`accepted_at`**: ถ้า null แปลว่ายังไม่ได้รับคำเชิญ

---

## 💻 ส่วนที่ 2: Backend (Laravel Controllers & Models)

รายละเอียดทั้งหมดครอบคลุม:
- Model: UserInvitation.php (มี methods: isExpired, isAccepted, isValid)
- Controller: AdminUserController.php (invite, resendInvite)
- Controller: InviteController.php (show, accept)
- Mail: UserInvitationMail.php
- Email Template: invitation.blade.php

---

## 🌐 ส่วนที่ 3: Routes

เพิ่ม routes ใน `routes/web.php`:
- Public: `/invite/{token}` (GET), `/invite/accept` (POST)
- Admin: `/admin/users`, `/admin/users/invite` (GET/POST)

---

## 🎨 ส่วนที่ 4: Frontend Components

- **InviteUser.jsx**: ฟอร์มสำหรับ Admin เชิญผู้ใช้ (มี Smart Logic ตาม role)
- **AcceptInvitation.jsx**: หน้าสำหรับผู้ใช้ตั้งรหัสผ่าน
- **UserList.jsx**: แสดงรายการผู้ใช้และคำเชิญที่รอดำเนินการ

---

## ✅ ส่วนที่ 5: การทดสอบ

### ขั้นตอนที่ 1: Database Setup
```bash
php artisan migrate
php artisan tinker
>>> Schema::hasTable('user_invitations') // ต้องเป็น true
```

### ขั้นตอนที่ 2-4: ทดสอบ Flow
- Admin ส่งคำเชิญ → เช็ค log file
- ผู้ใช้คลิกลิงก์ → กรอกฟอร์ม
- Login ด้วยบัญชีใหม่

### ขั้นตอนที่ 5: Edge Cases
- Token หมดอายุ
- Token ถูกใช้แล้ว
- ส่งคำเชิญซ้ำ

---

## 🔐 ความปลอดภัย

- ✅ Token Hashing (SHA-256)
- ✅ Expiration (72 ชั่วโมง)
- ✅ Single Use Token
- ✅ CSRF Protection
- ✅ Password Hashing

---

## ⏱️ เวลาที่คาดว่าจะใช้: ~7 ชั่วโมง

## 🎯 พร้อมเริ่มทำงานได้เลย!
